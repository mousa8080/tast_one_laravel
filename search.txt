🧠 ما هي الـ Session؟

الـ Session هي وسيلة لتخزين معلومات المستخدم مؤقتًا على السيرفر أثناء زيارته للموقع.
يعني لو المستخدم سجّل الدخول، ممكن نخزن اسمه أو الـ ID بتاعه في Session بحيث نقدر نستخدمه في أي صفحة تانية.
المعلومة بتفضل موجودة طول ما المستخدم فاتح المتصفح (أو لحد ما تنتهي الـ session).

🔸 تتخزن على السيرفر.
🔸 تنتهي لما المستخدم يقفل المتصفح أو بعد وقت معين.

🍪 ما هي الـ Cookie؟

الـ Cookie هي وسيلة لتخزين بيانات المستخدم على جهازه (المتصفح) نفسه.
مثلاً ممكن نخزن اسم المستخدم عشان لما يرجع يلاقيه مكتوب أو يكون عامل "تذكرني".

🔸 تتخزن في المتصفح.
🔸 ممكن نحدد ليها مدة صلاحية طويلة.

⚖️ الفرق بين Session و Cookie
العنصر	Session	Cookie
مكان التخزين	على السيرفر	في المتصفح
الأمان	أكثر أمانًا	أقل أمانًا
مدة التخزين	مؤقتة	ممكن نحددها بمدة طويلة
الاستخدام	بيانات حساسة (زي ID المستخدم)	بيانات عامة (زي اسم المستخدم أو اللغة)
💻 مثال عملي على Session و Cookie
<?php
// نبدأ الـ Session
session_start();

// نخزن بيانات في الـ Session
$_SESSION['username'] = "Mousa";

// نخزن بيانات في Cookie لمدة 1 يوم
setcookie("user", "MousaCookie", time() + 86400, "/");

echo "Session username: " . $_SESSION['username'] . "<br>";
echo "Cookie username: " . $_COOKIE['user'] ?? 'Cookie not set';
?>

🔍 الشرح:

session_start() لازم يكون أول سطر قبل أي مخرجات HTML.

$_SESSION يخزن البيانات في السيرفر.

setcookie() ينشئ Cookie في المتصفح باسم "user".

تقدر تشوف الـ Cookie في أدوات المتصفح (Developer Tools → Application → Cookies).

📁 Include و Require في PHP
🧩 الفكرة:

الاتنين بيستخدموا لإضافة كود من ملف تاني داخل ملفك الحالي.

العنصر	include	require
في حالة الملف مش موجود	يطلع Warning ويكمل التنفيذ	يطلع Fatal Error ويوقف التنفيذ
الاستخدام المناسب	ملفات مش ضرورية	ملفات أساسية لازم تتواجد
💻 مثال يوضح الفرق:
📂 main.php
<?php
echo "Start of file<br>";

include("header.php"); // ملف ممكن ما يكونش مهم
require("config.php"); // ملف أساسي

echo "End of file<br>";
?>

📂 لو الملف مش موجود:

لو header.php مش موجود → يظهر Warning لكن السكربت هيكمل.

لو config.php مش موجود → يظهر Fatal Error والسكربت هيقف.